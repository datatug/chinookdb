{
  "name": "chinookdb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-data.mjs",
    "dev": "pnpm generate && astro dev",
    "build": "pnpm generate && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "node scripts/validate-data.mjs",
    "test:worker": "node --import tsx/esm scripts/test-worker.ts"
  },
  "dependencies": {
    "astro": "^5.14.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^22.15.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2",
    "wrangler": "^4.38.0"
  }
}
